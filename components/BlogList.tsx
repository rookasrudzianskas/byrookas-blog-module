import React from 'react';

type Props = {
    posts: Post[];
};

const BlogList = ({posts}: Props) => {
    return (
        <div>

        </div>
    );
};

export default BlogList;
// by Rokas with ❤️
